//! `rsfai-detectors` — detector model and registry.
//!
//! Stub for M2. Will port the `pyFAI/detectors/_common.py` base (`pixel1`,
//! `pixel2`, `shape`, `get_pixel_corners`, registry) plus only the concrete
//! detectors the golden datasets need (`_dectris.py`: Pilatus1M/6M, Eiger4M)
//! and a generic `Detector(pixel1, pixel2, shape)`.
